<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Путь к папке с логами -->
    <property name="LOG_PATH" value="logs"/>

    <!-- Формат логов: дата, уровень, поток, класс, сообщение -->
    <property name="PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- Appender: запись всех логов в общий файл -->
<!--    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">-->
<!--        <file>${LOG_PATH}/app.log</file>-->
<!--        <encoder>-->
<!--            <pattern>${PATTERN}</pattern>-->
<!--        </encoder>-->
<!--        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">-->
<!--            &lt;!&ndash; Архивация: ротация каждый день или при достижении 10MB &ndash;&gt;-->
<!--            <fileNamePattern>${LOG_PATH}/archived/app-%d{yyyy-MM-dd}.%i.gz</fileNamePattern>-->
<!--            <maxFileSize>10MB</maxFileSize>-->
<!--            <maxHistory>7</maxHistory> &lt;!&ndash; Хранить 7 дней архивов &ndash;&gt;-->
<!--            <totalSizeCap>100MB</totalSizeCap>-->
<!--        </rollingPolicy>-->
<!--    </appender>-->

    <!-- Appender: только ошибки в отдельный файл -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/error.log</file>
        <encoder>
            <pattern>${PATTERN}</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archived/error-%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>50MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Appender: вывод в консоль (опционально) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Логгер для вашего пакета -->
    <logger name="com.example.armtek_parcer_new" level="INFO" additivity="false">
<!--        <appender-ref ref="FILE"/>-->
        <appender-ref ref="ERROR_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Корневой логгер -->
    <root level="INFO">
<!--        <appender-ref ref="FILE"/>-->
        <appender-ref ref="ERROR_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>